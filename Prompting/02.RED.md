# UnitConverter_14 — RED 단계 Transcript (02)

_Exported 2026-06-11 — 브랜치 `red` · Phase 1 Core RED_

> 상세 산출: `Report/03.REPORT.md`, `tests/test_*.py`, `.cursor/commands/tdd-red.md`

---

## 1. RED 시작

**User:** spec 완료 후 red 브랜치에서 Phase 1 Core RED 진행.

**Cursor:**

- `Phase: red | Track: B | Test ID: D-CNV-01` 선언
- `tests/test_domain.py` 생성 — `pytest.fail("RED: D-CNV-01 — …")`
- 커밋: `[RED] D-CNV-01 to_meter feet skeleton`

---

## 2. Track B — D-CNV-02 ~ 04

**User:** TC별 커밋 유지.

**Cursor:**

- D-CNV-02 ~ 04 순차 추가 (`convert_all`, feet→yard, yard 5자리)
- 각 커밋 후 `pytest tests/test_domain.py -v` → FAIL 확인

---

## 3. Track A — U-IN-01 ~ 05

**User:** Boundary RED 진행.

**Cursor:**

- `tests/test_boundary.py` 생성
- U-IN-01 ~ 05: 형식·음수·unknown·빈 토큰(parametrize)
- `tests/`만 수정, `UnitConverter.py` 미변경

---

## 4. U-OUT-01

**User:** U-OUT-01 RED.

**Cursor:**

- `meter:2.5` → stdout 2줄(feet·yard, 소수 1자리) RED 스켈레톤
- 커밋: `[RED] U-OUT-01 meter conversion output skeleton`

---

## 5. RED 완료 검증

```bash
python -m pytest tests/ -v
# 11 failed (U-IN-05 parametrize 포함)
```

**Gate 1 RED:** Phase 1 Core 10/10 RED ✅

---

## 6. RED 규칙 (세션 준수)

| 규칙 | 준수 |
|------|------|
| 구현 코드 금지 | ✅ |
| pytest.fail only | ✅ |
| 1 Test ID = 1 커밋 | ✅ |
| Track B → Track A 순서 | ✅ |

---

## 7. 다음 단계

- GREEN: `/tdd-green` — `converter.py`, `validator.py`, `UnitConverter.py`
- merge 순서: spec → **red** → green

[project]
name = "unit-converter-03"
version = "0.1.0"
description = "Length unit converter CLI — PRD-driven TDD (UnitConverter_03)"
requires-python = ">=3.11"

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
